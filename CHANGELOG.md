# pm-slop

## 0.1.0

### Minor Changes

- First release: `how-are-we-doing-on-this` (`/hawd`), `where-are-we-at-with-x` (`/wawat`), `can-we-ship-this-by-friday` (`/shipbf`). Installs on every agent that reads `SKILL.md` folders via `npx skills`, and as a Claude Code plugin from this repo's own marketplace.
